# Dependencies and build output
node_modules/
.next/
out/
coverage/

# Local environment and secrets
.env
.env.*
!.env.example
# Per-environment runtime values (single source on the operator machine,
# synced to the host by ./ops deploy; never committed except *.example)
deployment/env/*.env
!deployment/env/*.example

# Tooling
*.tsbuildinfo
.pnpm-store/

# OS and editors
.DS_Store
.idea/
.vscode/
